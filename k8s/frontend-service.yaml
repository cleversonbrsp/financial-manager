apiVersion: v1
kind: Service
metadata:
  name: financial-manager-frontend
  namespace: financial-manager
  labels:
    app: financial-manager-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: financial-manager-frontend

