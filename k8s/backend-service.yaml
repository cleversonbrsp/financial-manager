apiVersion: v1
kind: Service
metadata:
  name: financial-manager-backend
  namespace: financial-manager
  labels:
    app: financial-manager-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: financial-manager-backend

